import { describe, expect, it } from "vitest";

describe("cli example spec", () => {
  it("change this", async () => {
    const plaintext = "hello world";
    expect(plaintext).toBe("hello world");
  });
});
